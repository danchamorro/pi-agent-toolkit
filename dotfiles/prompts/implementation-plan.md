---
description: Convert an existing plan into committable implementation phases
argument-hint: "[paste plan]"
---

Convert the plan below into actionable, committable phases that can be tracked independently. For each phase, include tasks, dependencies, acceptance criteria, validation steps, and a suggested commit message. Use Markdown checkboxes for phase tasks so progress can be tracked directly in the file. Flag any unclear requirements or blockers. Write the result to a new Markdown file, choosing a clear filename based on the plan topic. Do not overwrite an existing file without asking first.

Plan:

$ARGUMENTS
